import PIAZZA_DOM_ENUM from '../constants/dom-enum';

const elements = new Set();

class BodyElement {
	static findElements(container=document.body) {

	}

	constructor(element) {
		this.element = element;
	}

	isPost() {

	}
}

export default BodyElement;